[build]
builder = "nixpacks"

[deploy]
startCommand = "java -jar backend/target/hr-management-system-1.0.0.jar --spring.profiles.active=prod"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
